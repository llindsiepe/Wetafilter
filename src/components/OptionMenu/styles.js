import * as React from "react";
import { styled } from "@mui/system";

export const Container = styled("button")({
  background: "none",
  color: "black",
  border: "none",
  borderRadius: 4,
  textAlign: "center",
  fontSize: "120%",
});
